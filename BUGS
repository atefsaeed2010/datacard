Known bugs

1) SMS sending charset problem

2) Terminate (from both side) active call break voice on all held

3) Terminate held call locally break voice on all active

4) Issue AT+CHLD=1x command before receiving ^CONF:call_idx after successfull ATDnum; 
kill device. Device not responding on commands but still sending ^BOOT: and ^RSSI: URCs

5) kill device after entering long PDU

6) white noise on x86_64

7) Some characters wrong encoded for SMS

8) command removed from queue by timeout when response already in read buffer

9) cpvt locking
